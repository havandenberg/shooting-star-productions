import React from 'react';
import l from '../ui/layout';

interface Props {
  toggleShowIntro: (showIntro: boolean) => void;
}

const Intro = ({ toggleShowIntro }: Props) => <l.Div />;

export default Intro;
